#include <stdio.h>

int main()
{
  int tam, num = 1, run, tri; scanf("%d", &tam);
  for (run = 0; run <= tam; run ++)
  {
    int soma[run];
    for (tri = 0; tri <= run; tri ++)
    {
      printf("%d\n", num);
      soma[tri] =
    }
  }
  return(0);
}
