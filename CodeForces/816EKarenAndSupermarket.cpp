#include <bits/stdc++.h>

int main()
{
  int n, b; scanf("%d %d", &n, &b);
  int items[n][3];
  scanf("%d %d", &items[i][0], &items[i][1]);
  for (int i = 0; i < n; i ++) for (int j = 0; j < 3; j ++)
    scanf("%d", &items[i][j]);

  int packages[n]

  return(0);
}
