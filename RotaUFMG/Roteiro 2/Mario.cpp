#include <bits/stdc++.h>

int main()
{
  int n, l;
  while (scanf("%d %d", &n, &l) && !(!n && !l))
  {
    int spaces[l];
    for (int i = 0; i < spaces; i ++)
      scanf("%d", &spaces[i]);


  }
  return(0);
}