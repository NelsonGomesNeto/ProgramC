#include <bits/stdc++.h>

int main()
{
  int first;
  while (scanf("%d", &first) && first != -1)
  {
    int other, biggest = first, sequence = 0, higgest;
    while (scanf("%d", &other) && other != -1)
    {

    }
  }
  return(0);
}